apiVersion: v1
kind: Service
metadata:
  name: {{ include "selectel_billing_exporter.fullname" . }}
  labels:
    {{- include "selectel_billing_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "selectel_billing_exporter.selectorLabels" . | nindent 4 }}
