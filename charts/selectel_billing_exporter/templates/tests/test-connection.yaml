apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "selectel_billing_exporter.fullname" . }}-test-connection"
  labels:
    {{- include "selectel_billing_exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "selectel_billing_exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
